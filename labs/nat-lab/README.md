# 🔒 Lab 4: NAT & ACL

## 📘 รายละเอียดโปรเจกต์  
การทดลองนี้เป็นการจำลองการทำงานของ **NAT (Network Address Translation)**  
และ **ACL (Access Control List)** เพื่อควบคุมสิทธิ์การเข้าถึงและแปลง IP ภายในให้ออกอินเทอร์เน็ต  
โดยใช้โปรแกรม **Cisco Packet Tracer**

---

## 🧩 สิ่งที่ศึกษา  
- การกำหนด NAT แบบ Static และ Dynamic  
- การสร้าง Access Control List เพื่อควบคุมการเข้าถึง  
- การทดสอบ Ping และจำลองการออกอินเทอร์เน็ตจากเครื่องภายใน  
- การตรวจสอบแพ็กเกตด้วย Simulation Mode  

---

## 🖼️ ภาพโครงสร้างเครือข่าย  
![NAT Topology](./nat-topology.png)

---

## ⚙️ ตัวอย่างคำสั่งบน Router  
```bash
Router(config)# interface g0/0
Router(config-if)# ip address 10.0.0.1 255.255.255.0
Router(config)# ip nat inside
Router(config)# interface g0/1
Router(config-if)# ip address 203.0.113.1 255.255.255.0
Router(config)# ip nat outside

! อนุญาตให้ NAT เฉพาะเครือข่ายภายใน
Router(config)# access-list 1 permit 10.0.0.0 0.0.0.255

! ตั้งค่า NAT แบบ Overload
Router(config)# ip nat inside source list 1 interface g0/1 overload
```

---
## 🧠 ผลลัพธ์การทดลอง
- อุปกรณ์ภายในสามารถออกอินเทอร์เน็ตได้ผ่าน NAT
- ACL สามารถจำกัดสิทธิ์การเข้าถึงได้ตามเงื่อนไข
- ระบบจำลองการเชื่อมต่อทำงานสมบูรณ์
---
## 📂 ไฟล์ประกอบ
- 🧩 NAT_Lab.pkt
- 🖼️ ภาพโครงสร้าง NAT & ACL
